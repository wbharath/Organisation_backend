spring.application.name=backend

spring.datasource.url=jdbc:mysql://localhost:3306/ems?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

#tells hibernate to automatically create the database tables if the tables are not exist in the database and also if there are ny changes in JPA Entities it will also change it in the database
spring.jpa.hibernate.ddl-auto=update
